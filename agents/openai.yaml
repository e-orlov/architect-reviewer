interface:
  display_name: "Architect & Reviewer"
  short_description: "V-model, risk, and AI complexity review"
  default_prompt: "Use $architect-reviewer to map this change through the V-model, apply proportionate risk and AI complexity gates where relevant, and issue an evidence-backed verdict."
  icon_small: "./assets/icon.svg"
  icon_large: "./assets/icon.svg"
policy:
  products:
    - "chatgpt"
    - "codex"
    - "api"
    - "atlas"
  allow_implicit_invocation: true
